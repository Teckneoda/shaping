# PAL Image Uploads Still Tied to Legacy CAPI — Services

## Summary table

| # | Service / Component | Repo | Change | Size |
|---|---|---|---|---|
| 1 | marketplace-graphql | marketplace-graphql | New `beginListingPhotoUpload` mutation (routes by vertical) + status poll query (Q21) + `services/listing-photos` client. No commit/edit mutations — edit = begin with `replaces` | M |
| 2 | listing-http-rest | marketplace-backend | CLASSIFIED signed-URL endpoint + **service-caller** multipart photo endpoints (add/replace/delete/delete-all; F4/F8) — no vertical concept needed anymore | M–L |
| 3 | `listing-ps-attach-photo` (NEW) | marketplace-backend | Classifieds attach subscriber — OBJECT_FINALIZE → authz → image-http-rest by ref (adPic1 first) → `$push` photo doc → status | M |
| 4 | Cars photo deployment (NEW) | marketplace-backend | **Separate, disposable cars deployment**: CAR signed-URL endpoint + CAR attach subscriber — direct write to Mongo `classifieds.auto` + `Public_CarsListingEvent` publish; retired when cars merges into classifieds | M–L |
| 5 | image-http-rest | marketplace-backend | Source-by-reference input + **priority/deferred rendition split** + renditions-PATCH service-JWT fix (promoted to required) | M |
| 6 | Staging bucket + Pub/Sub (NEW) | marketplace-backend `.tf` | Private bucket, POST CORS, lifecycle, IAM, **OBJECT_FINALIZE notification + topic + per-vertical subscriptions (required)** | S |
| 7 | ksl-api (legacy) | Legacy/ksl-api | **No changes** (hard constraint: no new endpoints); `storeListingImage` keeps serving old apps as-is | — |
| 8 | marketplace-frontend | marketplace-frontend | Server-action swap behind gate: begin → direct GCS POST → poll → final reorder; absorbs the Cars photo-editor branch | M |
| 9 | m-ksl-classifieds-api (legacy) | Legacy/m-ksl-classifieds-api | Upload/edit endpoints become shims; later retire photo controllers | S–M |
| 10 | feeds-ps-syncer | marketplace-backend | **Unchanged in v1** — but the F8 **synchronous multipart** contract is binding now; later one-line dispatcher swap | — |

---

## 1. marketplace-graphql — begin mutation + poll query + client

**Created:**
- `beginListingPhotoUpload` mutation (returns a signed upload POST policy; name matches the `begin*Upload` convention) → `{url, fields: JSON!, storageKey}`; `@hasRole(role: "MEMBER", useLowSecurity: true)`. Input carries `listingId, listingType, filename, contentType, position, replaces?, description?` — the resolver **routes by `listingType`** to the owning vertical's signed-URL endpoint (CLASSIFIED → listing-http-rest §2; CAR → cars deployment §4) and stays a thin proxy. Template: [mutation.job-apply-upload.graphqls](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-graphql/graph/schema/mutation.job-apply-upload.graphqls), resolver [job-apply-upload.go](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-graphql/graph/mutationresolvers/job-apply-upload.go). A **new mutation** — `beginProfileImageUpload` can't be reused (profile-pipeline backend/bucket + side effects, `mimeType`-only input that can't be extended additively, no `storageKey` in its response; see Features F1).
- **Status poll query** — the front end's "is it done?" signal (replaces the earlier commit mutation's synchronous response). Whether it reads the listing's photo array or a per-upload status doc (profiles precedent) is Q21; the shape must not preclude a later GraphQL subscription (F9 non-goal, deferred).
- ~~Commit mutation~~ / ~~Edit mutation~~ — **dropped (2026-09-01 async decision)**. Edit = `beginListingPhotoUpload` with `replaces` (Features F4). Note this changes the Q2 landscape: the `fields.Photos` silent no-op on the listing-rest path ([listing_mapping.go](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-graphql/graph/mutationresolvers/listing_mapping.go#L23-L138) never maps Photos) is no longer fixed by a commit mutation — it should be closed independently (document or reject the field).
- New client package `services/listing-photos/` following repo conventions: cookie forwarding [forwardAuthCookies](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-graphql/services/listing-rest/client.go#L750-L777), `applyServiceAuth` (ddm-jwt), sentinel errors + `ClientError`, spans `marketplace-graphql-<Client>-<Method>`.

**Constraints:** GraphQL layer proxies only (it never generates signed policies itself; no business logic — repo CLAUDE.md); `@hasRole` enforces MEMBER only, admin/on-behalf-of needs explicit checks (pattern: [resolveLifecycleMemberIDArg](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-graphql/graph/mutationresolvers/classifieds_listing_lifecycle.go#L119-L128)). Additive-only schema changes (mobile can't be force-updated). Decide routing-gate behavior (Q1). Repo uses spec-kit — this lands as `specs/003-*` with a `contracts/*.graphqls`.

## 2. listing-http-rest — CLASSIFIED signed-URL + service-caller endpoints

**Created:** CLASSIFIED get-signed-URL endpoint (generates the signed POST policy, pins the attach-contract metadata — Features F1), and the **service-caller multipart endpoints** (add, replace, per-photo delete, delete-all — Features F4 table; callers: feeds F8, jobs-migration, Capi shim, GraphQL `deletePhoto`). Reorder exists ([update.go:296-299](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-http-rest/internal/domain/update.go#L296-L299), `reorderPhotos` :394-419). **The browser add path does not call these** — it goes staging bucket → subscriber (§3).

**Structure:** the earlier `PhotoService` vertical registry is **dropped** — cars never enters this service (2026-09-01 decision; Features F4/F6), so listing-http-rest stays pure-Classifieds (store hard-bound to Mongo `classifieds.general*` is now fine). The attach logic should live in a package shareable with the §3 subscriber (one implementation, two triggers — Q25).

**Reused in place:** bouncer middleware + ownership policy + `requireVerifiedEmail`; `ImageClient` (extend beyond DeleteImage — [image.go:34-71](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-http-rest/internal/client/image.go#L34-L71)); cleanup-sidecar shape ([mediacleanup.go](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-http-rest/internal/sidecar/mediacleanup.go), [purgecleanup.go](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-http-rest/internal/sidecar/purgecleanup.go)); `Photo` type already models the full legacy doc ([listing.go:796-814](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-http-rest/internal/types/listing.go#L796-L814)).

**New responsibilities:** Classifieds rendition-list ownership (priority + deferred lists, Features F3); write-back failure reconciliation (image processed but listing write fails → orphaned image record); simplified-replace semantics (swap doc at index, delete old bytes only after — Features F4).

## 3. `listing-ps-attach-photo` — classifieds attach subscriber (NEW)

The CLASSIFIED leg of Features F2. Template: [listing-ps-update-video-url](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-ps-update-video-url/app.go#L86-L146). Consumes the staging bucket's OBJECT_FINALIZE events (own subscription), filters `vertical == CLASSIFIED`, then: verify staged object + ownership (model: [verifyStorageKeyFormat / verifyObjectOwnership](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-jobs-http-rest/internal/storage/storage.go#L213-L294)) → image-http-rest by reference with the priority rendition list → `$push` photo doc (dense-array invariant, Features F2) → surface status for the FE poll (Q21). Idempotent on staged object name = Pub/Sub redelivery guard (Q24). Handles `replaces` as the simplified atomic swap.

## 4. Cars photo deployment — separate, disposable (NEW)

**2026-09-01 decision (reverses Q5's 8/26 registry resolution):** cars photo handling is its **own deployment** in marketplace-backend — CAR signed-URL endpoint + CAR attach subscriber — per the meeting sketch ("two different deployments… eventually cars adopts the classifieds thing"). Deliberately disposable: retired when cars data merges into classifieds. Full operation list in Features F6. Highlights:
- **Ownership + existence natively**: `findOne` on `classifieds.auto` — same cluster/DB the platform already uses ([boosts maps cars→`auto`](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-cron-boosts/app.go#L64-L69); read precedent in prod: auto-renew, favorites). New: a **write grant** on `auto` for the deployment's Mongo user (Q16 — re-scoped from listing-http-rest's user to the new deployment's).
- **image-http-rest by reference** with the Cars rendition list ([rendition-recipes.go:58-116](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/media/services/image-http-rest/domain/renditions/rendition-recipes.go#L58-L116)) under the priority/deferred split; legacy-exact params: `vertical:'classifieds'`, Backlot `objectId` = auctionId string, `preMd5` of raw bytes.
- **Write-back**: `$push` the 10-field cars photo doc (+ `backlot.photoOrder`) via `findOneAndUpdate` ×3; replace = **one atomic pull+push** (change-stream/ES blanking hazard), old bytes deleted only after the swap (Q20); legacy-S3 deletions queue to `classifieds.photosToDelete`; `autoChangeLog` history insert.
- **Publish `Public_CarsListingEvent`** (full `cars.v4.Listing` from post-update doc; feeds BigQuery + Google Shopping, NOT ES). New work: the listing→proto mapping layer + publisher IAM on the externally-owned topic (Q17). Publisher plumbing itself is ~10 lines on the existing [pubsub publisher](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-http-rest/internal/pubsub/publisher.go).
- **ES is hands-off**: the [m-ksl-cars-api change-stream connector](file:///Users/cpies/code/shaping/Research%20Repos/Legacy/m-ksl-cars-api/connector/src/change-identifier.js#L36-L41) owns `cars-listings`; never write it directly.
- No ksl-api client needed for the photo path. Delete/reorder remain on existing ksl-api endpoints via GraphQL (can migrate later).

## 5. image-http-rest — source-by-reference + rendition split

**Updated:** `POST /image` gains a `{bucket, object}` source form alongside multipart (multipart permanent). Read primitive exists ([storage.go:162-194](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/services/media/shared-image-packages/storage/storage.go#L162-L194)); needs cross-bucket IAM (`objectViewer` on the staging bucket, per the jobs pattern). **Required (Q11 in v1):** the priority/deferred rendition split — cut the caller-named priority renditions (PAL: `classified/adPic1` + `classified/mobile_adPic1`) before returning, defer the rest via the add-renditions path; that path's **service-JWT rejection must be fixed** (memberId 0 hard-rejected: [app.go:663-674](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/media/services/image-http-rest/app.go#L663-L674)) — promoted from nice-to-have. Remaining nice-to-have fixes listed in Features.md.

## 6. Staging bucket + Pub/Sub (NEW infra)

Private GCS bucket + TF: uniform bucket-level access, POST/PUT/OPTIONS CORS with explicit ksl.com origins, `ConditionContentLengthRange` in policies, custom-time lifecycle delete (hours — orphans are the normal case), abort-incomplete-multipart. **Required (async decision): OBJECT_FINALIZE → Pub/Sub notification, one topic, one subscription per vertical subscriber (§3, §4).** Full template: [listing-jobs-http-rest/.tf/google-storage.tf](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-jobs-http-rest/.tf/google-storage.tf) and [.tf/pubsub.tf](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/listing/services/listing-jobs-http-rest/.tf/pubsub.tf). Staged bytes are attacker-controlled: never public, never served directly, content validated on read; subscribers trust only policy-pinned `x-goog-meta-*` metadata (Q22).

## 7. ksl-api (legacy) — no changes

**Hard constraint: no new ksl-api endpoints — and none are needed.** The Cars write-back is native in §4 (its legacy behavior is preserved by replicating the [storeListingImage side-effect map :2445-2645](file:///Users/cpies/code/shaping/Research%20Repos/Legacy/ksl-api/public_html/classifieds/cars/api/controllers/ListingController.php#L2445-L2645)). `storeListingImage` stays alive **unchanged** for old mobile apps — it already calls image-http-rest, and the native writer coexists safely under Mongo single-document atomicity. Existing `deleteImage` / `editListingJSON` keep serving delete/reorder via GraphQL. Quick win independent of the main build (a list edit, not a new endpoint): add `classified/adPic1` to the General rendition list ([ListingController.php:36-48](file:///Users/cpies/code/shaping/Research%20Repos/Legacy/ksl-api/public_html/classifieds/general/api/controllers/ListingController.php#L36-L48)).

## 8. marketplace-frontend — server-action swap

**Updated:** [addPhoto.ts](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-frontend/apps/ksl-marketplace/app/sell/%5B%5B...id%5D%5D/actions/addPhoto.ts) and [updatePhoto.ts](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-frontend/apps/ksl-marketplace/app/sell/%5B%5B...id%5D%5D/actions/updatePhoto.ts) → begin mutation / direct GCS upload / **poll until attached** (edit = begin with `replaces`), behind the rollout gate; one final `photoOrder` reorder after all tiles are done. Upload mechanics reuse the existing signed-POST helpers ([useUploadSlot.ts](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-frontend/apps/ksl-marketplace/app/listing/%5Bid%5D/components/Contact/useUploadSlot.ts), [VideoUploader.tsx](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-frontend/apps/ksl-marketplace/app/sell/post-a-listing/components/Video/VideoUploader.tsx) — video already polls, so the async UX has precedent). "Done" must mean **attached**, not merely uploaded — stepper blocking and tile draggability key off the poll (Features F5). Normalize the photo actions onto `authenticatedRequest` + Zod (DR-004) while touching them. Retires the legacy `/auth/` token exchange + nonce helpers for photos ([legacy-api-helpers.ts](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-frontend/apps/ksl-marketplace/app/sell/%5B%5B...id%5D%5D/utils/legacy-api-helpers.ts)).

**Cars photo editor (in flight):** the unmerged branch `feature-pal-use-new-photo-editor-sc-392495` enables the editor for Cars behind `CAR_pal_photo_editor` and fakes replace-in-place client-side (`replaceCarPhoto()` = add → delete → reorder, duplicate left behind on partial failure). Once the `replaces` flow exists, that fake should be replaced with the real atomic replace for CAR — coordinate merge order with sc-392495 (Q14).

## 9. m-ksl-classifieds-api (legacy) — shim then retire

**Updated:** `uploadListingPhoto`/`editListingPhoto` become shims onto the new path (bytes already in hand → **synchronous multipart** to the §2 service-caller endpoints or image-http-rest — old apps need a synchronous response, so the shims never route through the staging bucket). Photo controllers/helpers retire when old-app traffic allows. This also ends the four copy-pasted helper implementations (find/reorder/cleanup/validate) across Capi, ksl-api General, and ksl-api Cars.

## 10. feeds-ps-syncer — unchanged in v1, contract binding now

Photos continue via Capi upload (which becomes a shim). The migration is **already designed on the feeds side** — its specs and code label Capi photo upload and the ksl-api `DeleteAllPhotos` client as one-line-swap bridges awaiting a Listing Service photo endpoint ([upstream.go:9-13](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/feeds/services/feeds-ps-syncer/internal/routing/upstream.go#L9-L13), [photo_diff.go:12-19](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/feeds/services/feeds-ps-syncer/internal/domain/photo_diff.go#L12-L19), [specs/010 plan.md:201](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/specs/010-feed-system-p1-skeleton/plan.md#L201)). What binds THIS project is the **F8 endpoint contract — synchronous multipart, unaffected by the async browser transport** (bytes + `originalFilePath` round-trip, first=primary ordering, `photo[].id`+`originalFilePath` read-back, per-photo delete + delete-all, 4xx/5xx split, ddm-jwt auth). The swap itself is later feeds work; it retires feeds' `CapiAuth` + `internal/kslapi/` and lets the destructive nuke-and-reupload path become a targeted diff. Source-URL ingestion stays a separate measure-first decision (Q6, fetch-vs-upload failure-classification cost: [photos.go:130-142](file:///Users/cpies/code/shaping/Research%20Repos/marketplace-backend/apps/feeds/services/feeds-ps-syncer/internal/domain/photos.go#L130-L142)). Feeds is Classifieds-only — it will never exercise the CAR leg.
